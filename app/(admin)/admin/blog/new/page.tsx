import { PostEditor } from "../_components/post-editor";

export const metadata = { title: "New article — DreamRiver Admin" };

export default function NewPostPage() {
  return <PostEditor post={null} />;
}
